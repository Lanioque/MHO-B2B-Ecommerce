"use client"

// This component is kept for potential future use but hidden by default
// You can remove it if not needed, or customize it for branches/projects

export function NavProjects({
  projects,
}: {
  projects: {
    name: string
    url: string
    icon: any
  }[]
}) {
  // Hidden by default - can be enabled later if needed for projects/branches
  return null
}
